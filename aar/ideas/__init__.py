"""
Methods for the AAR harness.

Each subdirectory is a self-contained method following the submit-model contract:
`run_experiment(config) -> {"model_path": <dir>, "method": <name>}`. Start from
TEMPLATE/. The AAR writes new methods here at run time.
"""
